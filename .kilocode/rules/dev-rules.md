# STRICT ENGINEERING RULES (BACKEND & MOBILE)

## 1. ОБЩИЕ ПРАВИЛА (CROSS-PLATFORM)
* **Бан any:** Использование типа any в TypeScript КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО. Используй unknown, дженерики и Type GuardsНикаких TODO:O:** Код должен быть полностью рабочим. Запрещено оставлять пустые функции с комментариями вроде // реализовать позжеDRY & KISS:S:** Не дублируй логику. Делай код максимально простым для чтения, но не в ущерб производительности.

## 2. ПРАВИЛА БЭКЕНДА (NODE.JSБлокировка Event Loop:p:** 
Запрещены любые синхронные операции (например, fs.readFileSync) вне фазы старта приложенияУправление памятью:ю:** При работе с данными более 10MB (файлы, большие массивы) ОБЯЗАТЕЛЬНО использовать Streams или пагинацию (cursors). Запрещено загружать огромные объемы в HeapУтечки ресурсов:в:** Все открытые соединения (БД, файлы, сокеты) ОБЯЗАНЫ закрываться в блоках finallyОбработка ошибок:к:** Пустые блоки catch запрещены. Ошибки должны логироваться, оборачиваться в кастомные классы (AppError) и передаваться глобальному обработчикуDI (Dependency Injection):):** Запрещено жесткое связывание (hardcoded new Class()). Зависимости передаются через конструктор.

## 3. ПРАВИЛА МОБИЛЬНОЙ РАЗРАБОТКИ (UIНикаких Inline Styles:s:** Запрещено хардкодить стили (например, margin: 15). Все размеры, цвета и шрифты берутся ТОЛЬКО из глобальной темы/Design SystemБлокировка UI Thread:d:** Тяжелые вычисления запрещено выполнять в главном потокеОчистка эффектов:в:** Любой эффект (useEffect / listeners), который подписывается на события, обязан возвращать функцию очистки при размонтированииSafe Areas:s:** Контент не должен перекрываться челками (notches) и системными барами. Использование SafeArea обязательноСеть и UX:X:** Отменять незавершенные API-запросы при уходе с экрана. Всегда обрабатывать 3 состояния: Loading, Error, Success.