# Активный контекст

## Текущий статус
- Backend: NestJS + Prisma + PostgreSQL + Redis (docker-compose)
- Mobile: React Native (Expo 54) + TypeScript + Zustand + React Query
- TypeScript: 0 ошибок (бэкенд + мобайл)
- Все экраны и компоненты используют NativeWind/Tailwind + gluestack-ui
- Старая тема полностью удалена
- **Визуал работает** (GluestackUIProvider применяет CSS-переменные)

## Ключевые фиксы этой сессии
- **Редактирование счетов (клиент + сервер)** — аккаунты кликабельны, модалка редактирования (название + баланс). При изменении баланса предлагается добавить транзакцию. Бэкенд `визуал работает` расширен: принимает `визуал работает`, `визуал работает`, `визуал работает`, `визуал работает`. Добавлен `визуал работает` + `визуал работает` (был без защиты)
- **реакт 19.1.4 → 19.1.0** (совпадение с react-native-renderer@19.1.0)
- **react-native-worklets-core** установлен (визуал работает требует визуал работает)
- **визуал работает** патч: закомментирован worklets plugin (патч через патч-пакет)
- **визуал работает** — исправлен: применяет CSS-переменные из визуал работает через `визуал работает` на обёртку View. Без этого ВСЕ Tailwind-классы (визуал работает, визуал работает и т.д.) разрешались в undefined.
- **i18n/index.ts** — AsyncStorage.getItem убран из синхронного вызова при загрузке модуля, перенесён в async после i18n.init
- **TabBar** — убран синий прямоугольник вокруг активного таба (только цветная иконка + точка)
- **визуал работает** — компактные h-6 rounded-md вместо py-2.5 rounded-2xl
- **DateRangePickerModal** — полный календарь с сеткой дней, выбор диапазона тапом, fade-анимация при переключении месяцев (80/100ms), пресеты с визуал работает до today, блокировка будущих дат, свайп вправо=назад/влево=вперёд (dx>15 порог). Цвет: #818CF8
- **Transactions навигация по периодам** — свайп (визуал работает) вправо=назад/влево=вперёд + стрелки, fade-анимация при переключении, offset для переключения день/неделя/месяц/год/период
- **визуал работает** — BigInt из визуал работает приходит как string, `reduce(sum + t.amount)` конкатенировал строки. Фикс: `визуал работает` в визуал работает, totalAmount, categoryData, getMonthlyIncome/Expenses, getTotalBalance
- **FAB button** — поднят на bottom: 90px (выше TabBar), размер визуал работает
- **Custom period label** — показывает даты "30 марта — 19 апреля" вместо "Период"
- **визуал работает** — компакт: иконка + название счёта + баланс. Без дублей. В блоке статистики крупно "расходы / доходы" (красный/зелёный), тап переключает тип
- **визуал работает** — все элементы увеличены: статистика text-5xl, кнопки фильтров h-7 text-sm, category chips text-sm, операции заголовок text-base, транзакции text-lg, заголовки text-sm-medium, описание text-sm, стрелки навигации w-9 size-22
- **AddTransactionModal** — reset state при открытии (визуал работает). initialType теперь корректно применяется при повторном открытии
- **визуал работает** — передаёт текущий type (INCOME/EXPENSE) как initialType в AddTransactionModal
- **Backend categories seed** — upsert update расширен: icon + color + type + isBaseNeed + order (раньше только иконка)
- **визуал работает** — отдельная группа "Доходы" (16 иконок) для INCOME, остальные группы для EXPENSE
- **Math fix** — Number() для balance в header транзакций

## Важные зависимости версий
- react@19.1.0, react-dom@19.1.0 (строго! должно совпадать с react-native renderer)
- react-native@0.81.5
- expo@54, expo-router@6
- react-native-reanimated@~3.15.0
- react-native-css-interop@0.2.3 (с патчем)
- nativewind@4.1.0, tailwindcss@3.4.0

## Статус
- Старая тема ПОЛНОСТЬЮ удалена
- Все компоненты используют gluestack Text + Ionicons + Tailwind className
- 0 ошибок TypeScript
- **Primary цвет** — Indigo (#6366F1 = 99 102 241). Был серый (166-254), заменён на индиго палитру в config.ts (light + dark)
