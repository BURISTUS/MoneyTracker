# Активный контекст

## Текущая задача
Нет активной задачи. Memory Bank инициализирован.

## Статус
- Проект запущен, основная архитектура выстроена
- Backend: NestJS + Prisma + PostgreSQL + Redis (docker-compose)
- Mobile: React Native (Expo 54) + TypeScript + Zustand + React Query
- Основные модули работают: Auth, Accounts, Categories, Transactions, Wishlist, Life-Cost
- Демо-режим работает (isDemoMode флаг)
